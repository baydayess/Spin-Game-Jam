using UnityEngine;

public class Slot_Color : MonoBehaviour
{
    public enum slot_Color
    {
        black,
        red,
        green
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
